<%= simple_form_for([current_user,@subscribe]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :heading %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
